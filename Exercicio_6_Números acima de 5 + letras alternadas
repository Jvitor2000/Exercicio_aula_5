#include <stdio.h>

int main() {
    int vetor[6] = {2, 8, 4, 10, 3, 7};

    printf("Numeros maiores que 5: ");

    for(int i = 0; i < 6; i++) {
        if(vetor[i] > 5) {
            printf("%d ", vetor[i]);
        }
    }

    return 0;
}
